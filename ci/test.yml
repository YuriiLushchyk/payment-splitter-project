---
platform: linux

image_resource:
  type: docker-image
  source: {repository: java, tag: openjdk-8}

inputs:
  - name: git-resource

caches:
  -path: .gradle/

run:
  path: git-resource/ci/test_script.sh